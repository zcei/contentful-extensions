{
  "name": "<EXTENSION_ID>",
  "version": "1.0.0",
  "description": "UI extension",
  "main": "dist/index.html",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "yarn clean && parcel build src/index.html --public-url /extension/<EXTENSION_ID>",
    "clean": "rm -rf dist/",
    "dev": "yarn clean && parcel src/index.html --https"
  },
  "devDependencies": {
    "parcel-bundler": "^1.7.1"
  }
}
